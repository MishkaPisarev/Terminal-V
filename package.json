{
  "name": "terminal-v",
  "version": "0.1.0",
  "private": true,
  "description": "High-complexity financial platform demonstrating advanced architecture",
  "scripts": {
    "dev": "pnpm --filter terminal-web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "type-check": "pnpm -r type-check"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
